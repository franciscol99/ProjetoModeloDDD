@model ProjetoModeloDDD.MVC.ViewModels.GerenciadorDeTarefasViewModel

@{
    ViewBag.Title = "Remover tarefa";
    ViewBag.ID = Model.GerenciadorDeTarefasID;
    ViewBag.BtnVoltar = true;
    ViewBag.BtnEditar = true;
}

<h3 class="text-danger">
    Tem certeza que deseja deletar a tarefa "@Html.DisplayFor(model => model.GerenciadorDeTarefasID) - @Html.DisplayFor(model => model.Descricao)
    <span class="badge text-bg-info">@Html.DisplayFor(model => model.DataVencimento) </span>"?
</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DataVencimento)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DataVencimento)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <button type="submit" class="btn btn-danger">Remover</button>
        </div>
    }
</div>
