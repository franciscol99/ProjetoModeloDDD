@model IEnumerable<ProjetoModeloDDD.MVC.ViewModels.MusicaViewModel>

@{
    ViewBag.Title = "Músicas";
    ViewBag.PlaceHolderPesquisar = "(ID, Nome, Autor, Gravadora)";
    ViewBag.Pesquisar = true;
    ViewBag.BtnCadastro = true;
    ViewBag.BtnExtra = new string[] {
       " <a href=\"?P=PlayListRandom\" class=\"btn btn-totvs\">PlayList aleatória</a>",
       " <a href=\"?P=MusicaRandom\" class=\"btn btn-totvs\">Música aleatória</a>",
       " <a href=\"/"+@ViewContext.RouteData.Values["controller"].ToString()+"\" class=\"btn btn-totvs\">Reset</a>",
   };

}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MusicaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Autor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gravadora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lancamento)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MusicaID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Autor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gravadora)
            </td>
            <td>
                @Convert.ToDateTime(item.Lancamento).ToString("dd/MM/yyyy")
            </td>
            <td class="td-menu">
                @Html.ActionLink("Editar", "Editar", new { id = item.MusicaID }, new { @class = "btn btn-light btn-sm" })
                @Html.ActionLink("Detalhes", "Detalhes", new { id = item.MusicaID }, new { @class = "btn btn-light btn-sm" })
            </td>
        </tr>
    }

</table>

<div class="alert alert-info alert-dismissible fade show" role="alert">
    <h4 class="alert-heading">Métodos</h4>
    <p>Funções disponíveis na página <strong>@ViewBag.Title</strong></p>
    <hr>
    <ol class="list-group list-group-numbered">
        <li class="list-group-item d-flex justify-content-between align-items-start">
            <div class="ms-2 me-auto">
                <div class="fw-bold">Listagem/Cadastrar/Editar/Detalhes/Remover/Pesquisar</div>
                Por padrão, é possível Cadastrar, Editar, Remover, Detalhar e Pesquisar itens que são mostrados em uma listagem na página inicial. Para remover é necessário
                entrar em "Editar" ou "Detalhes".
            </div>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-start">
            <div class="ms-2 me-auto">
                <div class="fw-bold">Lista aleatória</div>
                Clicando no botão "PlayList aleatória", retornará apenas uma lista de músicas, de maneira aleatória
            </div>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-start">
            <div class="ms-2 me-auto">
                <div class="fw-bold">Música aleatória</div>
              Clicando no botão "Música aleatória", retornará apenas uma música na lista, de maneira aleatória
            </div>
        </li>

    </ol>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
